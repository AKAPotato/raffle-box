# 更新日志

本项目是单文件应用，版本号同时维护在三处：仓库根 `VERSION`、`index.html` 里的 `APP_VERSION` 常量、
以及界面右上角的显示。三者始终一致。

## v1.0.2（2026-09-23）

**新增功能** + **修复 12 个问题**，其中大部分集中在座位抽取的视觉表现与抽取过程的动画体验。
**无配置格式变更**，v1.0.1 的名单 / 员工表 / 设置可直接沿用。

**升级方式**：下载新的 `index.html` 覆盖旧文件即可（浏览器打开后可强制刷新一次）。

### 新增

1. **打开座位抽取即显示座位图，默认 10 排 10 座**
   以前第一次切到座位抽取时图上空空如也，要先动一下参数才画出来。
   现在一进来就是一张 10×10 的座位图，可以直接看着它抽。

2. **「历史已抽」出现时有了专属动效**
   上一轮抽中的座位在下一轮转为「历史已抽」时，会平滑换成已抽配色，
   并伴随删除线由左向右划出，一眼能看到"这个座位刚被划掉"。
   动效只在状态真的变化的那一帧播放，改排数等重绘不会反复播。

### 修复

**抽取过程与动画**

1. **滚动还没结束，本次结果就被标成了「历史已抽」**
   抽取结果在动画开始前就已记账，导致座位在滚动期间就被画上删除线 —— 换个样式照样提前剧透。
   现在滚动期间座位保持原样，揭晓瞬间才高亮。

2. **座位图每次抽取都会闪一下消失**
   抽取瞬间整块座位图被隐藏、620ms 后才重新出现。现在改为全程可见。

3. **抽干后再点一次抽取，整张座位图凭空消失**
   现在会重绘为只剩历史「已抽中」标记的空图，而不是整块不见。

4. **快速「停止 → 再抽取」会把上一轮结果重复写进历史**
   手动停止模式下两轮动画交叠时，旧结果会再落一次账（B 站奖品也会被多扣一份）。已加令牌守卫。

5. **改「排数」等参数时，中奖座位会反复弹跳**
   座位图每次重绘都会重建 DOM，弹跳动画被反复触发。现在只在「本次刚抽中」时播一次。

6. **中奖座位在动画结束后会"糊一下再变清晰"**
   中奖格静止态挂着小数倍缩放，文字需要等浏览器重新按最终分辨率栅格化才清晰。
   现在静止态不带缩放，改用描边环 + 阴影表达"突出"，文字全程清晰。

7. **第一排（以及最左/最右列）的中奖座位描边被裁掉一截**
   座位图是横向滚动容器，超出容器的装饰会被裁切。已按动画峰值预留出足够空间。

8. **系统开启「减少动态效果」后，部分动画照旧播放**
   卡片位移、座位弹跳、按钮脉冲、扫光等已一并纳入该偏好。

**抽取逻辑**

9. **B 站「评论 + 转发合并」模式会把同一个人算成两个人**
   既评论又转发的用户会被当成两条独立候选，中奖概率翻倍，同一轮还可能被抽中两次。
   现在按 UID 去重，评论条目优先保留。

10. **B 站跨次抽取时同一用户可能重复中奖**
    记录已抽用户时漏记了「同一人有多个条目」的标识，导致跨轮次去重失效。

11. **座位起始排号 / 起始座位号在部分路径下不生效**
    同一份几何计算在两处各写了一遍（结果池与座位图），改动只落到其中一处时两者会对不上。
    现在统一由同一个函数计算。

12. **抽取按钮会把奖品名当 HTML 解析**
    奖品名里含 `<img src=x onerror=...>` 时，按钮里会真的插入元素。已转义，超长奖品名也不再撑成两行。

**界面一致性**

13. **图例里「历史已抽」的图标与实际样式不符**
    图例画的是斜线，实际座位格是横贯整格的删除线。已统一为横线。

14. **导入表格时分隔符可能识别错**
    首行含制表符 / 逗号 / 分号混合时，原来按固定优先级判定，可能误判。现在按首行最优候选判定。

### 验证

- 9 轮端到端自检共 **409 项断言全部通过**（主流程 61 + 边界与解析 58 + 辅助模块 60 + 竞态与导出 42
  + 随机参数模糊测试 120 组 + 界面自洽 13 + 分组/权重/员工/B 站 32 + 界面与动画专项 24），0 运行时异常。
- 动画类问题改为在**动画的特定时刻**读取界面状态来断言（滚动中 / 揭晓后 / 重绘后），
  避免只在动画结束后检查而漏判。

---

## v1.0.1（2026-09-22）

稳定性更新：修复 7 个问题，全部来自端到端自检中发现的路径。**无新增功能、无配置格式变更**，
旧版本的名单 / 员工表 / 设置可直接沿用。

**升级方式**：下载新的 `index.html` 覆盖旧文件即可（浏览器打开后可强制刷新一次）。

### 修复

1. **粘贴不带协议头的 B 站短链会中断拉取**
   输入 `b23.tv/xxx` 这类短链时解析会抛出异常，整个评论拉取流程直接中断。
   现在会自动补全 `https://`，无法识别的输入也会给出明确提示而不是崩溃。

2. **B 站抽奖显示的剩余人数虚高，还会自相矛盾**
   关闭「每人只算一次」时，同一用户的多条评论会被当成不同的人计数，
   出现「剩余 4 / 共 4」但下一轮却提示「都已抽完」的情况。
   现在改为按「人」计数，同一用户在一次抽取中只占一个中奖名额，缺额由其他未中签用户补齐。

3. **员工表「重新导入」后抽不到人**
   换一张表后，上一张表的部门筛选条件仍然保留，而新表里通常没有该部门，候选池直接变空。
   现在重新导入会一并清空部门筛选。

4. **参数填错时的提示不准确**
   最小值填了非数字，提示却是「请先完成参数设置」，看不出到底哪里错了。
   现在会明确提示「请输入有效数字」。

5. **权重为 0 的奖项仍会被抽中**
   「权重 0」的设计意图是永不中奖，但当剩余候选的权重都为 0 时，随机算法会固定抽中第一项
   —— 抽取数量越接近奖项数量，越必然触发。
   现在这种情况不再强行补位，并提示「剩余候选项权重均为 0」或「N 个名额未抽出」。

6. **导出的 CSV 在 Excel 里会错行**
   导出使用 CRLF 换行，但单元格转义规则漏掉了单独的回车符，内容里含回车时表格会串列。
   现在回车、换行、逗号、双引号都会正确转义。

7. **座位抽取后点「立即重置」，座位图不刷新**
   「重置已抽」按钮会重绘座位图，但抽完后的「立即重置」没有，座位图上仍标着旧的中奖座位。
   现在两个入口共用同一套重置逻辑。

### 验证

- 3 轮端到端自检共 **179 项断言全部通过**（主流程 61 + 边界与解析 58 + 辅助模块 60）。
- 覆盖 7 种抽取模式的主流程与边界、B 站链接解析、CSV 导入导出、权重算法、
  历史记录、奖品队列、偏好持久化、手动停止竞态、XSS 注入防护。

---

## v1.0.0（2026-09-21）

首个正式版本。

- **7 种抽取模式**：随机数字、座位抽取、名单抽签、员工信息抽取、分组抽签、权重概率抽奖、B 站评论抽奖
- **B 站评论助手**：16 路并行抓取、扫码登录取全量评论，零外部依赖（二维码库已内嵌）
- **零依赖单文件**：主程序仅一个 `index.html`，双击即用，不联网也能完成除 B 站抓取外的全部功能
- **细节**：明暗主题、中奖礼花、历史记录、结果复制与 CSV 导出、跨次不重复抽取
- **离线环境**：附带 Node.js 离线安装包说明，便于在无网络机器上运行助手
- 许可证：MIT
