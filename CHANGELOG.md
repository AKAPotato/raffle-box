# 更新日志

本项目为单文件应用，版本号同时维护在仓库根目录的 `VERSION`、`index.html` 中的 `APP_VERSION` 常量
以及界面右上角的版本标识三处，三者始终保持一致。

## v1.0.3（2026-09-24）

本次更新修复 18 项已知问题，主要集中在跨模板切换时的记账一致性、界面交互的时序响应以及数值边界处理。
本次更新不涉及配置项与数据格式变更，v1.0.2 的名单、员工表与偏好设置可直接沿用。

**升级方式**：下载新版 `index.html` 覆盖原文件即可，浏览器打开后建议强制刷新一次。

### 修复

**抽取过程与结果记账**

1. **抽取后随即切换模板，历史记录被记为新模板名称**
   揭晓回调延后执行，记账时读取的是已变更的当前模板，导致本轮结果被写入其他模板的历史。
   现改为：批次结果与其所属模板一并暂存，卡片移除前先完成记账，遗留回调同时作废。

2. **手动停止滚动期间切换模板，本轮结果未计入历史**
   取消滚动时仅停止动画，未对已扣减的名额做收尾处理，历史记录与 B 站奖品队列均未更新。
   现改为：取消滚动前先完成本轮记账。

3. **跨模板场景下 B 站奖品队列多扣或漏扣**
   奖品扣减经由按当前模板写入的通道执行，模板切换后扣减落到错误位置，队列随后不再变化。
   现改为：扣减显式写入 B 站模板，奖品文本仅按换行拆分，不再受名称中的逗号影响。

4. **切换模板后立即抽取，结果被延时复位回调整块清除**
   模板字段区延后重建，其复位回调无条件重绘空状态，将重建期间刚生成的结果一并清除。
   现改为：存在未展示的结果时不再重绘空状态。

5. **座位图沿用上一批次的中奖标记**
   模板切换时结果清空的时机晚于记账，上一批次的中奖座位被标注到新模板的座位图上。
   现改为：先清空结果再完成记账。

**界面响应与文件操作**

6. **模板切换后短时间内执行抽取，误报「请输入有效数字」**
   字段默认值尚未写入状态，抽取入口按空值判定为非法输入。现改为：抽取前补齐当前模板的默认值。

7. **字段区重建完成前的输入被丢弃**
   输入事件在下一轮任务中挂载，渲染与该回调之间的一次输入不会生效。现改为：随字段同步挂载。

8. **导出 CSV 在部分浏览器中偶发失败或生成空文件**
   下载触发后随即释放对象 URL，部分浏览器尚未开始读取该文件。现改为：延后释放。

9. **再次导入同一文件时不触发任何响应**
   文件选择控件未重置，选中同一文件时不再产生变更事件，重新导入无效果。现改为：读取完成后清空控件值。

10. **结果卡片移除后礼花动画在界面左上角触发**
    礼花位置延后计算，此时卡片可能已脱离文档，坐标取到零值。现改为：卡片已不在文档中时不再播放。

11. **切换模板后 B 站扫码登录轮询仍持续运行**
    登录状态轮询未判断当前模板，切换后仍按原周期继续请求。现改为：离开 B 站模板即终止轮询。

**抽取逻辑与数值边界**

12. **数字区间端点为小数时绕过枚举上限，界面失去响应**
    枚举上限仅按整数端点判定，填写 `0 ~ 1000000000.5` 一类区间时会尝试枚举全部候选值。
    现改为：按实际可选值个数判定，超出上限时改为按需生成。

13. **大整数区间可能生成落在区间之外的数值**
    随机值取整的表达式在端点非整数时会将下界一并舍入。现改为：先取整再叠加到下界。

14. **小数候选接近抽尽时抽取响应明显变慢**
    拒绝采样未先收敛抽取上限，剩余候选不足时长时间空转。现改为：按剩余数量收敛上限，
    候选耗尽时直接提示重置；判重同时改用哈希集合。

15. **数字区间规模较大时抽取卡顿**
    候选池一次性全量构造并整体洗牌，耗时随区间规模线性增长。现改为：超大区间按需生成，
    洗牌仅覆盖实际抽取的前若干位。

16. **抽取数量未设上限**
    数量字段超出候选规模较多时，结果区渲染开销过大，界面失去响应。现改为：单次抽取最多 2000 个，
    超出时给出提示并按上限执行。

17. **座位总数未设上限**
    排数与每排座位数取较大值时，座位图渲染开销过大，界面失去响应。现改为：座位总数最多 100000 个，
    超出时在对应字段提示。

18. **名单条目名称包含逗号时权重被丢弃**
    权重按首个分隔符拆分，姓名或备注中含逗号时权重段解析失败并回退为默认值。
    现改为：优先取末段作为权重，其余部分合并为名称。

---

## v1.0.2（2026-09-23）

本次更新新增 2 项功能，修复 14 项已知问题，主要集中在座位抽取的视觉呈现与抽取过程的动画体验。
本次更新不涉及配置项与数据格式变更，v1.0.1 的名单、员工表与偏好设置可直接沿用。

**升级方式**：下载新版 `index.html` 覆盖原文件即可，浏览器打开后建议强制刷新一次。

### 新增

1. **座位抽取默认渲染座位图**
   首次进入座位抽取时即渲染座位图，默认规模为 10 排 × 10 座，无需先调整参数。

2. **「历史已抽」状态切换动效**
   上一轮抽中的座位在下一轮转为「历史已抽」时，配色平滑过渡，并伴随删除线由左向右划出。
   动效仅在座位状态发生变化时播放，因调整参数引起的重绘不会重复触发。

### 修复

**抽取过程与动画**

1. **抽取结果在滚动阶段即被标记为「历史已抽」**
   结果在动画开始之前完成记账，导致座位在滚动阶段即显示删除线，中奖信息在揭晓前泄露。
   现已调整：滚动阶段座位保持可选状态，揭晓时统一高亮。

2. **抽取过程中座位图短暂隐藏**
   抽取瞬间座位图被整体隐藏，约 620ms 后重新显示，出现可见的闪烁。现全程保持可见。

3. **候选耗尽后再次抽取，座位图不再显示**
   抽完最后一个座位后再次点击抽取，座位图整块消失。
   现改为重绘为仅保留历史「已抽中」标记的空图。

4. **手动停止后快速再次抽取，上一轮结果被重复写入历史**
   手动停止模式下两轮动画交叠时，上一轮结果会再次记账，B 站奖品也会被重复扣减。已加入令牌校验。

5. **调整参数时中奖座位反复播放弹跳动画**
   座位图每次重绘都会重建 DOM，导致弹跳动画被重复触发。现仅在「本次抽中」时播放一次。

6. **中奖座位在动画结束后短暂模糊**
   中奖格在静止状态下带有小数倍缩放，文字需等待浏览器按最终分辨率重新栅格化才恢复清晰。
   现静止状态不再使用缩放，改由描边环与阴影体现选中状态，文字全程清晰。

7. **首行及首末列的中奖描边被裁切**
   座位图为横向滚动容器，超出容器的装饰元素会被裁切。已按动画峰值预留足够的显示空间。

8. **系统「减少动态效果」偏好未完全生效**
   卡片位移、座位弹跳、按钮脉冲与扫光等动画现已一并纳入该偏好的覆盖范围。

**抽取逻辑**

9. **B 站「评论 + 转发合并」模式下同一用户被重复计入候选**
   既评论又转发的用户被计为两条独立候选，中奖概率翻倍，且同一轮中可能被重复抽中。
   现按 UID 去重，评论条目优先保留。

10. **B 站跨次抽取时同一用户可能重复中奖**
    记录已抽用户时遗漏了「同一用户存在多条记录」的标识，导致跨轮次去重失效。

11. **座位起始排号与起始座位号在部分路径未生效**
    同一份几何计算在结果池与座位图两处各自实现，改动只落到其中一处时两者结果不一致。
    现已统一由同一函数计算。

12. **抽取按钮将奖品名按 HTML 解析**
    奖品名包含 HTML 标签时会被解析为实际元素。现已转义，超长奖品名以单行省略显示，不再撑高按钮。

**界面一致性**

13. **图例「历史已抽」标识与座位格实际样式不一致**
    图例使用斜线，而座位格实际为横贯整格的删除线。已统一为横线。

14. **导入表格时分隔符判定有误**
    首行同时包含制表符、逗号与分号时，原按固定优先级判定可能出错。现按首行最优候选判定。

---

## v1.0.1（2026-09-22）

稳定性更新，修复 7 项已知问题，均来自端到端自检中发现的路径。本次更新不涉及配置项与数据格式变更，
旧版本的名单、员工表与偏好设置可直接沿用。

**升级方式**：下载新版 `index.html` 覆盖原文件即可，浏览器打开后建议强制刷新一次。

### 修复

1. **粘贴不带协议头的 B 站短链会导致拉取中断**
   输入 `b23.tv/xxx` 形式的短链时解析抛出异常，评论拉取流程随之中断。
   现自动补全 `https://`；无法识别的输入将返回明确提示，不再导致流程崩溃。

2. **B 站抽奖的剩余人数统计虚高且前后矛盾**
   关闭「每人只算一次」时，同一用户的多条评论被计为多名候选，出现「剩余 4 / 共 4」
   而下一轮却提示「均已抽完」的情况。现改为按用户计数，同一用户在一次抽取中仅占一个中奖名额，
   缺额由其他未中签用户补齐。

3. **员工表重新导入后无法抽取**
   更换数据表后，上一张表的部门筛选条件被保留，而新表通常不含该部门，候选池因此为空。
   重新导入时将一并清除部门筛选。

4. **参数校验提示不准确**
   最小值为非数字时提示「请先完成参数设置」，无法定位具体错误项。现已明确提示「请输入有效数字」。

5. **权重为 0 的奖项仍可能被抽中**
   「权重 0」的语义为永不中奖，但当剩余候选权重均为 0 时，随机算法会固定命中首项，
   且抽取数量接近奖项数量时必然触发。现已不再强制补位，并提示「剩余候选项权重均为 0」
   或「N 个名额未抽出」。

6. **导出的 CSV 在 Excel 中错行**
   导出使用 CRLF 换行，但单元格转义规则遗漏了单独的回车符，内容含回车时表格串列。
   现已对回车、换行、逗号与双引号统一转义。

7. **座位抽取后使用「立即重置」，座位图未刷新**
   「重置已抽」按钮会重绘座位图，而抽取完成后的「立即重置」入口未执行重绘，
   座位图仍保留上一轮的中奖标记。两个入口现已共用同一套重置逻辑。

---

## v1.0.0（2026-09-21）

首个正式版本。

- **7 种抽取模式**：随机数字、座位抽取、名单抽签、员工信息抽取、分组抽签、权重概率抽奖、B 站评论抽奖
- **B 站评论助手**：16 路并行抓取，扫码登录获取全量评论，无外部依赖（二维码库已内嵌）
- **零依赖单文件**：主程序仅一个 `index.html`，双击即可使用；除 B 站抓取外，全部功能可离线运行
- **功能细节**：明暗主题、中奖礼花、历史记录、结果复制与 CSV 导出、跨次不重复抽取
- **Node.js 指引**：README 附官网安装链接与说明，便于为 B 站评论抽奖配置本地助手
- 许可证：MIT
